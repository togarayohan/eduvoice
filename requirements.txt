# ==========================================
# EduVoice: Headless Offline AI Tutor
# Dependencies for PC / Raspberry Pi
# ==========================================

# Core LLM Engine (Gemma 4 via Ollama API)
requests>=2.31.0

# Unsloth & Fine-tuning (Only needed if running training scripts)
# unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git
# torch
# xformers

# Ear: Speech-to-Text (Whisper Offline)
openai-whisper>=20231117
sounddevice>=0.4.6
pyaudio>=0.2.14
scipy>=1.11.0
numpy>=1.24.0

# Brain: Local RAG (Curriculum Grounding)
chromadb>=0.4.22
sentence-transformers>=2.2.2

# Voice: Kokoro TTS (Natural Offline Voice)
kokoro>=0.1.0
# Direct Audio Playback (Required for Headless mode)
pydub>=0.25.1
simpleaudio>=1.0.4

# Fallback TTS
pyttsx3>=2.90

# Utilities
pathlib2>=2.3.7
